<?xml version="1.0" encoding="utf-8"?>
<!--
  The wall is the WebView; the connecting panel sits on top of it and is shown
  only when even the disk cache cannot draw (rule nine, layer three). Keeping
  both in one frame means the status can appear and vanish without tearing down
  the WebView underneath, so a recovered load is instant.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/wall_background">

    <WebView
        android:id="@+id/web_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- The native "connecting…" status. Branded, never blank. Starts hidden;
         the activity shows it on a cold boot and on a load that cannot fall
         back to cache. -->
    <LinearLayout
        android:id="@+id/connecting"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="32dp"
        android:background="@color/wall_background"
        android:visibility="gone">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/app_name"
            android:textColor="@color/wall_accent"
            android:textSize="28sp"
            android:textStyle="bold" />

        <ProgressBar
            style="?android:attr/progressBarStyle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:indeterminateTint="@color/wall_accent" />

        <TextView
            android:id="@+id/connecting_host"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:textColor="@color/wall_text"
            android:textSize="16sp"
            android:gravity="center" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="@string/connecting_hint"
            android:textColor="@color/wall_text_dim"
            android:textSize="13sp"
            android:gravity="center" />

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginTop="28dp">

            <Button
                android:id="@+id/retry"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/retry" />

            <Button
                android:id="@+id/change_server"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="12dp"
                android:text="@string/change_server" />
        </LinearLayout>
    </LinearLayout>
</FrameLayout>
